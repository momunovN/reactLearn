import React from 'react';
import ReactDOM from 'react-dom/client';
import IndexMain from './app/index-main';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <IndexMain/>
  </React.StrictMode>
);

